import * as auth from "@/database/schema/auth";
import * as media from "@/database/schema/media";
import * as community from "@/database/schema/community";

export const schema = {
  auth,
  media,
  community,
};
